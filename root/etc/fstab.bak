# /etc/fstab: static file system information.
#
# See the manpage fstab(5) for more information.
#
# NOTE: The root filesystem should have a pass number of either 0 or 1.
#       All other filesystems should have a pass number of 0 or greater than 1.
#
# NOTE: Even though we list ext4 as the type here, it will work with ext2/ext3
#       filesystems.  This just tells the kernel to use the ext4 driver.
#
# NOTE: You can use full paths to devices like /dev/sda3, but it is often
#       more reliable to use filesystem labels or UUIDs. See your filesystem
#       documentation for details on setting a label. To obtain the UUID, use
#       the blkid(8) command.

# <fs>			<mountpoint>				<type>		<opts>				<dump> <pass>
/dev/sda2               /efi            			vfat            defaults                        0 2
/dev/nvme0n1p1          /               			xfs             defaults,noatime                0 1
/dev/nvme0n1p2          none            			swap            sw                              0 0
/dev/sda1               /home           			btrfs           defaults,subvol=@		0 0
/dev/sda1               /home/gravity   			btrfs           defaults,subvol=gravity		0 0
/dev/sda1               /home/gravity/Games  			btrfs           defaults,subvol=gravity/Games	0 0
/dev/sda1               /home/gravity/.local/share/Trash   	btrfs           defaults,subvol=gravity/.local/share/Trash		0 0

# tmpfs		/var/tmp/portage		tmpfs	size=4G,uid=portage,gid=portage,mode=775	0 0
#/dev/cdrom		/mnt/cdrom	auto		noauto,ro	0 0

# 192.168.100.97:/data   /data nfs4 _netdev,rw,rsize=1048576,wsize=1048576,vers=4

COMMON_FLAGS="-march=skylake -mtune=skylake -O2 -pipe -flto=8"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

CC="gcc"
CPP="cpp"
CXX="g++"
AR="ar"
NM="nm"
RANLIB="ranlib"

FEATURES="${FEATURES}"
